// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrl: 'http://ec2-3-27-16-39.ap-southeast-2.compute.amazonaws.com/api/v1',
  urlEndpoint: 'https://ik.imagekit.io/quangduyle',
  urlUpload: 'https://upload.imagekit.io/api/v1/files/upload',
  publicKey: 'public_YlnrhfcKb4S+ctsAjbI8LorF314=',
  authenticationEndpoint: 'http://localhost:4200',
  imagekitID: 'quangduyle',

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
