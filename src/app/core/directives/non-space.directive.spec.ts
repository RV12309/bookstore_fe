import { NonSpaceDirective } from './non-space.directive';

describe('NonSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NonSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
